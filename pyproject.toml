[project]
name = "yuki-bot"
version = "0.1.0"
description = "private telegram bot — long-term life companion"
requires-python = ">=3.11"
dependencies = [
    "libsql-client==0.3.1",
    "python-dotenv==1.0.1",
    "dateparser==1.2.0",
    "httpx==0.27.2",
]

[tool.vercel]
entrypoint = "api.index:handler"
